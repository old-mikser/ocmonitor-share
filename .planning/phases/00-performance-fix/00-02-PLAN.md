---
phase: 00-performance-fix
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - ocmonitor/utils/file_utils.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "FileProcessor.load_all_sessions() accepts optional start_date/end_date parameters"
    - "FileProcessor pre-filters by directory mtime when date range provided"
    - "Secondary in-memory filter checks actual session start_time date"
  artifacts:
    - path: "ocmonitor/utils/file_utils.py"
      provides: "FileProcessor.load_all_sessions() with date filtering"
      min_lines: 40
  key_links:
    - from: "FileProcessor.load_all_sessions()"
      to: "session directories"
      via: "mtime pre-filter + start_time secondary filter"
      pattern: "start_ts.*end_ts"
---

<objective>
Add optional date filtering to FileProcessor.load_all_sessions() with pre-filtering by directory mtime and secondary in-memory filter by session start_time.

Purpose: Provide equivalent date filtering for file-based sessions, using mtime for quick pre-filtering and start_time for accurate secondary filtering.
Output: Modified FileProcessor.load_all_sessions() accepting start_date and end_date parameters.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@ocmonitor/utils/file_utils.py

<!-- Key interface for implementation -->
<!-- From file_utils.py:350-371 -->
```python
@staticmethod
def load_all_sessions(base_path: str, limit: Optional[int] = None) -> List[SessionData]:
```

<!-- Key: Secondary filter uses TimeUtils.date_in_range() - import from time_utils -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add date filtering to FileProcessor.load_all_sessions()</name>
  <files>ocmonitor/utils/file_utils.py</files>
  <behavior>
    - Test 1: load_all_sessions(path, start_date=date(2025,1,1), end_date=date(2025,1,7)) filters sessions by directory mtime first, then by actual session start_time
    - Test 2: load_all_sessions(path) without dates returns all sessions (backward compatible)
    - Test 3: load_all_sessions(path, limit=5) with date filter applies limit AFTER filtering
  </behavior>
  <action>
    Add `start_date: Optional[date] = None` and `end_date: Optional[date] = None` parameters to `load_all_sessions()` method signature.

    After finding session directories but BEFORE limit is applied:
    - If start_date or end_date provided, convert to timestamps:
      - `start_ts = start_date.timestamp() if start_date else 0`
      - `end_ts = end_date.timestamp() if end_date else float('inf')`
    - Pre-filter session_dirs list to only include directories where `start_ts <= d.stat().st_mtime <= end_ts`

    After loading session data, apply secondary in-memory filter:
    - Check if session.start_time exists
    - Get session_date = session.start_time.date()
    - Use TimeUtils.date_in_range(session_date, start_date, end_date) to determine if session should be included
    - Skip sessions outside range

    Import `TimeUtils` from `..utils.time_utils` for the secondary filter check.
  </action>
  <verify>
    <automated>cd /home/mikser/ocmonitor-share && python -m pytest tests/unit/test_file_utils.py::TestLoadAllSessions -x -q</automated>
  </verify>
  <done>FileProcessor.load_all_sessions() accepts optional start_date/end_date, pre-filters by mtime, applies secondary in-memory filter by session start_time</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→filesystem | Untrusted date input, session directory access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-00-02 | Information Disclosure | Session directories | accept | Only accesses directories user owns; path is base_path limited |
| T-00-03 | Denial of Service | Large number of sessions | accept | Limit param still applies after filtering |
</threat_model>

<verification>
- Unit test for load_all_sessions with date filter (tests/unit/test_file_utils.py)
</verification>

<success_criteria>
- FileProcessor.load_all_sessions() signature includes start_date and end_date optional parameters
- Pre-filter by mtime reduces directories scanned when date range provided
- Secondary filter checks actual session start_time date (not mtime)
- Existing callers work unchanged (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/00-performance-fix/00-02-SUMMARY.md`
</output>
