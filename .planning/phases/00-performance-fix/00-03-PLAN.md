---
phase: 00-performance-fix
plan: '03'
type: execute
wave: 1
depends_on:
  - "00-01"
  - "00-02"
files_modified:
  - ocmonitor/utils/data_loader.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "DataLoader.load_all_sessions() accepts optional start_date/end_date parameters"
    - "DataLoader passes date parameters to SQLiteProcessor or FileProcessor based on source"
  artifacts:
    - path: "ocmonitor/utils/data_loader.py"
      provides: "DataLoader.load_all_sessions() with date filtering propagated to underlying processors"
      min_lines: 30
  key_links:
    - from: "DataLoader.load_all_sessions()"
      to: "SQLiteProcessor.load_all_sessions()"
      via: "start_date, end_date params passed through"
      pattern: "SQLiteProcessor\\.load_all_sessions.*start_date"
    - from: "DataLoader.load_all_sessions()"
      to: "FileProcessor.load_all_sessions()"
      via: "start_date, end_date params passed through"
      pattern: "FileProcessor\\.load_all_sessions.*start_date"
---

<objective>
Propagate optional date filtering through DataLoader.load_all_sessions() to underlying SQLite or File processors.

Purpose: Provide unified date filtering entry point that delegates to appropriate processor based on data source.
Output: Modified DataLoader.load_all_sessions() accepting start_date and end_date parameters.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@ocmonitor/utils/data_loader.py

<!-- Key interface for implementation -->
<!-- From data_loader.py:138-163 -->
```python
def load_all_sessions(self, limit: Optional[int] = None) -> List[SessionData]:
    source = self._determine_source()
    if source == "sqlite":
        sessions = SQLiteProcessor.load_all_sessions(self._resolved_db_path, limit)
    else:
        sessions = FileProcessor.load_all_sessions(str(self._resolved_files_path), limit)
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagate date params through DataLoader.load_all_sessions()</name>
  <files>ocmonitor/utils/data_loader.py</files>
  <behavior>
    - Test 1: load_all_sessions(start_date=date(2025,1,1), end_date=date(2025,1,7)) passes dates to SQLiteProcessor when source is sqlite
    - Test 2: load_all_sessions(start_date=date(2025,1,1), end_date=date(2025,1,7)) passes dates to FileProcessor when source is files
    - Test 3: load_all_sessions() without dates returns all sessions (backward compatible)
  </behavior>
  <action>
    Add `start_date: Optional[date] = None` and `end_date: Optional[date] = None` parameters to `load_all_sessions()` method signature.

    Import `date` from `datetime` module (already imported).

    Pass date parameters to appropriate processor:
    - For SQLiteProcessor: pass as keyword args `start_date=start_date, end_date=end_date`
    - For FileProcessor: pass as keyword args `start_date=start_date, end_date=end_date`

    Note: SQLiteProcessor uses datetime objects (converted to ms internally), FileProcessor uses date objects directly for mtime comparison.
  </action>
  <verify>
    <automated>cd /home/mikser/ocmonitor-share && python -c "from ocmonitor.utils.data_loader import DataLoader; dl = DataLoader(); print('DataLoader.load_all_sessions accepts date params:', 'start_date' in str(dl.load_all_sessions.__code__.co_varnames))"</automated>
  </verify>
  <done>DataLoader.load_all_sessions() accepts optional start_date/end_date, passes to appropriate processor</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataLoader→Processors | Trusted internal calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-00-04 | Tampering | Processor calls | accept | Internal method calls, not external input |
</threat_model>

<verification>
- Import check passes
- Method signature includes date parameters
</verification>

<success_criteria>
- DataLoader.load_all_sessions() signature includes start_date and end_date optional parameters
- Date params passed to SQLiteProcessor when source is sqlite
- Date params passed to FileProcessor when source is files
- Existing callers work unchanged (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/00-performance-fix/00-03-SUMMARY.md`
</output>
