---
phase: 00-performance-fix
plan: '04'
type: execute
wave: 1
depends_on:
  - "00-03"
files_modified:
  - ocmonitor/services/session_analyzer.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "SessionAnalyzer.analyze_all_sessions() accepts optional start_date/end_date parameters"
    - "SessionAnalyzer passes date parameters to DataLoader.load_all_sessions()"
  artifacts:
    - path: "ocmonitor/services/session_analyzer.py"
      provides: "SessionAnalyzer.analyze_all_sessions() with date filtering"
      min_lines: 20
  key_links:
    - from: "SessionAnalyzer.analyze_all_sessions()"
      to: "DataLoader.load_all_sessions()"
      via: "start_date, end_date params passed through"
      pattern: "self\\._data_loader\\.load_all_sessions.*start_date"
---

<objective>
Propagate optional date filtering through SessionAnalyzer.analyze_all_sessions() to DataLoader.

Purpose: Provide high-level entry point for date-filtered session analysis used by report generation.
Output: Modified SessionAnalyzer.analyze_all_sessions() accepting start_date and end_date parameters.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@ocmonitor/services/session_analyzer.py

<!-- Key interface for implementation -->
<!-- From session_analyzer.py:51-63 -->
```python
def analyze_all_sessions(self, base_path: Optional[str] = None, limit: Optional[int] = None) -> List[SessionData]:
    return self._data_loader.load_all_sessions(limit)
```

<!-- Note: date is already imported (line 6) -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagate date params through SessionAnalyzer.analyze_all_sessions()</name>
  <files>ocmonitor/services/session_analyzer.py</files>
  <behavior>
    - Test 1: analyze_all_sessions(start_date=date(2025,1,1), end_date=date(2025,1,7)) passes dates to DataLoader
    - Test 2: analyze_all_sessions() without dates returns all sessions (backward compatible)
  </behavior>
  <action>
    Add `start_date: Optional[date] = None` and `end_date: Optional[date] = None` parameters to `analyze_all_sessions()` method signature.

    Pass date parameters to DataLoader.load_all_sessions() as keyword arguments: `start_date=start_date, end_date=end_date`.
  </action>
  <verify>
    <automated>cd /home/mikser/ocmonitor-share && python -c "from ocmonitor.services.session_analyzer import SessionAnalyzer; print('analyze_all_sessions has date params:', 'start_date' in str(SessionAnalyzer.analyze_all_sessions.__code__.co_varnames))"</automated>
  </verify>
  <done>SessionAnalyzer.analyze_all_sessions() accepts optional start_date/end_date, passes to DataLoader</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analyzer→DataLoader | Trusted internal calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-00-05 | Tampering | DataLoader calls | accept | Internal method calls, not external input |
</threat_model>

<verification>
- Import check passes
- Method signature includes date parameters
</verification>

<success_criteria>
- SessionAnalyzer.analyze_all_sessions() signature includes start_date and end_date optional parameters
- Date params passed to DataLoader.load_all_sessions()
- Existing callers work unchanged (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/00-performance-fix/00-04-SUMMARY.md`
</output>
