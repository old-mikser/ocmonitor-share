---
phase: 00-performance-fix
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - ocmonitor/utils/sqlite_utils.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "SQLiteProcessor.load_all_sessions() accepts optional start_date/end_date parameters"
    - "SQLiteProcessor.load_all_sessions() adds WHERE clause to SQL when dates provided"
    - "Date filtering uses correct millisecond timestamp conversion for SQLite"
  artifacts:
    - path: "ocmonitor/utils/sqlite_utils.py"
      provides: "SQLiteProcessor.load_all_sessions() with date filtering"
      min_lines: 50
  key_links:
    - from: "SQLiteProcessor.load_all_sessions()"
      to: "SQLite session table"
      via: "WHERE s.time_created BETWEEN ? AND ?"
      pattern: "WHERE s\\.time_created"
---

<objective>
Add optional date filtering to SQLiteProcessor.load_all_sessions() to push filtering to database level.

Purpose: Eliminate O(n) message loading for sessions outside date range by filtering at SQL query level.
Output: Modified SQLiteProcessor.load_all_sessions() accepting start_date and end_date parameters.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@ocmonitor/utils/sqlite_utils.py

<!-- Key interface for implementation -->
<!-- From sqlite_utils.py:248-289 -->
```python
@classmethod
def load_all_sessions(
    cls, db_path: Optional[Path] = None, limit: Optional[int] = None
) -> List[SessionData]:
```

The SQLite `time_created` column stores milliseconds since epoch.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add date filtering to SQLiteProcessor.load_all_sessions()</name>
  <files>ocmonitor/utils/sqlite_utils.py</files>
  <behavior>
    - Test 1: load_all_sessions(db_path, start_date=date(2025, 1, 1), end_date=date(2025, 1, 7)) returns only sessions created in that range
    - Test 2: load_all_sessions(db_path, start_date=None, end_date=date(2025, 1, 7)) returns sessions created up to that date
    - Test 3: load_all_sessions(db_path, start_date=date(2025, 1, 1), end_date=None) returns sessions created from that date onwards
    - Test 4: load_all_sessions(db_path) without dates returns all sessions (backward compatible)
  </behavior>
  <action>
    Add `start_date: Optional[date] = None` and `end_date: Optional[date] = None` parameters to `load_all_sessions()` method signature.

    Build WHERE clause dynamically:
    - If both dates provided: `WHERE s.time_created BETWEEN ? AND ?`
    - If only start_date: `WHERE s.time_created >= ?`
    - If only end_date: `WHERE s.time_created <= ?`

    Convert Python date objects to milliseconds since epoch:
    - start_date: `int(datetime.combine(start_date, datetime.min.time()).timestamp() * 1000)`
    - end_date: `int(datetime.combine(end_date, datetime.max.time()).timestamp() * 1000)`

    Append WHERE clause to the SQL query before LIMIT clause.
  </action>
  <verify>
    <automated>cd /home/mikser/ocmonitor-share && python -m pytest tests/unit/test_sqlite_utils.py -x -q 2>/dev/null || echo "Tests require SQLite DB with test data"</automated>
  </verify>
  <done>SQLiteProcessor.load_all_sessions() accepts optional start_date/end_date, SQL query includes WHERE clause when dates provided, backward compatible with existing callers</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→SQLite DB | Untrusted date input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-00-01 | Injection | SQLite query | mitigate | Parameterized WHERE clause with `?` placeholders, dates converted to milliseconds (integers) not strings |
| T-00-02 | Denial of Service | Large date range | accept | SQLite handles range queries efficiently; application-level limit param still applies |
</threat_model>

<verification>
- Unit test for load_all_sessions with date filter (tests/unit/test_sqlite_utils.py)
- Integration test for `--week` flag using SQLite (tests/integration/test_cli.py)
</verification>

<success_criteria>
- SQLiteProcessor.load_all_sessions() signature includes start_date and end_date optional parameters
- SQL query builds WHERE clause correctly for all date combinations (both, start only, end only, neither)
- Timestamp conversion produces correct millisecond values for SQLite comparison
- Existing callers work unchanged (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/00-performance-fix/00-01-SUMMARY.md`
</output>
