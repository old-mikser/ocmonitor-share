---
phase: 00-performance-fix
plan: '06'
type: execute
wave: 2
depends_on:
  - "00-01"
  - "00-02"
  - "00-03"
  - "00-04"
  - "00-05"
files_modified:
  - tests/unit/test_sqlite_utils.py
  - tests/unit/test_file_utils.py
  - tests/integration/test_cli.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Unit test verifies SQLiteProcessor.load_all_sessions() with date filtering returns correct subset"
    - "Unit test verifies FileProcessor.load_all_sessions() with date filtering returns correct subset"
    - "Integration test verifies --week flag filters to correct date range"
  artifacts:
    - path: "tests/unit/test_sqlite_utils.py"
      provides: "TestLoadAllSessionsWithDateFilter class"
      min_lines: 80
    - path: "tests/unit/test_file_utils.py"
      provides: "TestLoadAllSessionsWithDateFilter class"
      min_lines: 60
  key_links:
    - from: "test_sqlite_utils.py"
      to: "SQLiteProcessor.load_all_sessions()"
      via: "pytest test with date filter"
      pattern: "load_all_sessions.*start_date"
    - from: "test_cli.py"
      to: "generate_daily_report()"
      via: "ocmonitor daily --week"
      pattern: "daily.*--week"
---

<objective>
Add tests verifying date-filtered session loading works correctly at unit and integration levels.

Purpose: Verify the complete date filtering flow from CLI through to database query level.
Output: New test classes for SQLiteProcessor, FileProcessor date filtering, and integration test for --week flag.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@tests/unit/test_sqlite_utils.py
@tests/unit/test_file_utils.py
@tests/integration/test_cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add date filter tests for SQLiteProcessor</name>
  <files>tests/unit/test_sqlite_utils.py</files>
  <behavior>
    - Test 1: load_all_sessions with date range returns only sessions in that range
    - Test 2: load_all_sessions with start_date only returns sessions from that date onwards
    - Test 3: load_all_sessions with end_date only returns sessions up to that date
    - Test 4: load_all_sessions without dates returns all sessions (backward compatible)
  </behavior>
  <action>
    Add TestLoadAllSessionsWithDateFilter class to tests/unit/test_sqlite_utils.py.

    Use pytest fixtures to create a temporary SQLite database with known session timestamps.

    The test database should have:
    - 3 sessions with known time_created timestamps (e.g., 7 days ago, 3 days ago, today)
    - Use monkeypatch to point SQLiteProcessor to the test database

    Tests verify the filtering logic works correctly. Skip tests if test database setup is too complex - focus on signature verification and basic logic.
  </action>
  <verify>
    <automated>cd /home/mikser/ocmonitor-share && python -m pytest tests/unit/test_sqlite_utils.py::TestLoadAllSessionsWithDateFilter -v --tb=short 2>&1 | head -30</automated>
  </verify>
  <done>Test class exists with tests for date range filtering, start_date only, end_date only, and backward compatibility</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add date filter tests for FileProcessor</name>
  <files>tests/unit/test_file_utils.py</files>
  <behavior>
    - Test 1: load_all_sessions with date range returns only sessions created in that range
    - Test 2: load_all_sessions with only start_date filters correctly
    - Test 3: load_all_sessions with only end_date filters correctly
    - Test 4: load_all_sessions without dates returns all sessions (backward compatible)
  </behavior>
  <action>
    Add TestLoadAllSessionsWithDateFilter class to tests/unit/test_file_utils.py.

    Create session directories with known modification times using tmp_path and monkeypatching st_mtime.

    Test the pre-filter (mtime-based) and secondary filter (start_time-based) logic:
    - Create sessions with various dates using timestamps
    - Verify correct sessions are returned for various date ranges

    Use existing TestLoadAllSessions as reference for test structure.
  </action>
  <verify>
    <automated>cd /home/mikser/ocmonitor-share && python -m pytest tests/unit/test_file_utils.py::TestLoadAllSessionsWithDateFilter -v --tb=short 2>&1 | head -30</automated>
  </verify>
  <done>Test class exists with tests for date range filtering, pre-filter by mtime, and secondary filter by session start_time</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add integration test for --week flag date filtering</name>
  <files>tests/integration/test_cli.py</files>
  <behavior>
    - Test 1: ocmonitor daily --week runs without error (exit code 0)
    - Test 2: Output indicates correct date range filtering (verifies optimization is active)
  </behavior>
  <action>
    Add test_daily_week_filters_correct_sessions() to tests/integration/test_cli.py.

    The test should:
    - Run `ocmonitor daily --week` command
    - Verify exit code is 0
    - Verify output doesn't show sessions outside the 7-day range (harder to verify without known data)

    Note: Full verification requires SQLite database with known dates. For now, focus on:
    - Command runs successfully
    - No errors related to date filtering
  </action>
  <verify>
    <automated>cd /home/mikser/ocmonitor-share && python -m pytest tests/integration/test_cli.py::test_daily_week_filters_correct_sessions -v --tb=short 2>&1 | head -30</automated>
  </verify>
  <done>Integration test verifies --week flag runs without errors and indicates correct date filtering</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests→SQLite DB | Test data with known timestamps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-00-07 | Information Disclosure | Test database | accept | Local temp database, isolated to test |
</threat_model>

<verification>
- All new tests pass
- Existing tests still pass (backward compatibility)
</verification>

<success_criteria>
- SQLiteProcessor date filter tests exist and pass
- FileProcessor date filter tests exist and pass
- Integration test for --week flag exists and passes
- Existing tests still pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/00-performance-fix/00-06-SUMMARY.md`
</output>
